language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t susah80/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true susah80/react-test npm run test

after_success:
  - docker build -t susah80/multi-client ./client
  - docker build -t susah80/multi-nginx ./nginx
  - docker build -t susah80/multi-server ./server
  - docker build -t susah80/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push susah80/multi-client
  - docker push susah80/multi-nginx
  - docker push susah80/multi-server
  - docker push susah80/multi-worker

deploy:
  edge: true
  provider: elasticbeanstalk
  region: us-east-2
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-east-2-823759707142
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
